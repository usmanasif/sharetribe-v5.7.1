/ Latest compiled and minified CSS
.header-top 
  .header-holder 
    .logo 
      %a{:href => "http://still-ridge-7153.herokuapp.com/"} Vendor Advisor
    - if @current_user.present?
      %ul#myTopnav.topnav
        %li
          .dropdown
            %button.dropbtn{:onclick => "myFunction()"} Properties
            %ul#myDropdown1.dropdown-content
              %li
                %a{:href => "http://still-ridge-7153.herokuapp.com/businesses/search_business"} Add Property
              - unless @current_user.is_admin?
                %li.divider
                %li
                  %a{:href => "http://still-ridge-7153.herokuapp.com/businesses"} My Properties
              - else
                %li
                  %a{:href => "http://still-ridge-7153.herokuapp.com/businesses"} Properties
        - if @current_user.is_admin?
          %li
            %a{:href => "http://still-ridge-7153.herokuapp.com/businesses/brand_list"} Brands
          %li
            %a{:href => "http://still-ridge-7153.herokuapp.com/businesses/vendor_list"} Vendors
        - else
          %li
            .dropdown
              %button.dropbtn{:onclick => "myFunction1()"} Vendors
              %ul#myDropdown2.dropdown-content
                %li
                  %a{:href => "http://still-ridge-7153.herokuapp.com/businesses/search_vendor"} Add Vendor
                %li
                  %a{:href => "http://still-ridge-7153.herokuapp.com/vendors"} My Vendors
          %li
            %a{:href => "/"} MarketPlace
          %li
            %a{:href => "http://still-ridge-7153.herokuapp.com/chat"} Chat
      %ul#myTopnav.topnav.nav-right-top 
        %li
          .dropdown
            %button.dropbtn{:onclick => "myFunction2()"} ABD
            = @current_user.given_name.slice(0,1).capitalize + @current_user.given_name.slice(1..-1)
            %ul#myDropdown3.dropdown-content
              %li= link_to "My Profile", "http://still-ridge-7153.herokuapp.com/users/edit"
              %li= link_to "Logout", "http://still-ridge-7153.herokuapp.com/users/logout", :method => 'delete'
     
:css
  .header-top {
    font-family: 'Helvetica';
    border-color: #080808;
    background-color: #222;
    border-color: #080808;
    position: relative;
    min-height: 50px;
    font-size: 14px;
    border: 1px solid transparent;
  }
  .header-holder {
    width: 1170px;
    margin: 0 auto 0;
  }

  .logo {
    float: left;
    padding: 15px 15px;
    font-size: 18px;
    line-height: 20px;
    height: 50px;
  }
  .logo a {
    color: #9d9d9d;
    text-decoration: none;
  }
  .logo a:focus,
  .logo a:hover {
      color: #fff;
      background-color: transparent;
  }
  ul.topnav {
      list-style-type: none;
      margin: 0 ;
      padding: 0;
      float: left;
      
  }
  header-holder:after,
  ul.topnav:after {
    display:block;
    clear:both;
    content:'';
  }
  /* Float the list items side by side */
  ul.topnav li {
    float: left;
    position: relative;
  }
  /* Style the links inside the list items */
  ul.topnav li a {
      color: #9d9d9d;
      text-align: center;
      text-decoration: none;
      line-height: 20px;
      position: relative;
      display: block;
      padding: 15px 15px ;
  }

  /* Change background color of links on hover */
  ul.topnav li a:hover {
    background-color: #555;
  }
  ul.topnav li a:hover {
    color: #fff;
    background-color: transparent;
  }
  ul.topnav li a:hover:focus {
    color: #fff;
    background-color: transparent;
  }

  /* Hide the list item that contains the link that should open and close the topnav on small screens */
  ul.topnav li.icon {display: none;}
  /* When the screen is less than 680 pixels wide, hide all list items, except for the first one ("Home"). Show the list item that contains the link to open and close the topnav (li.icon) */
  /* Dropdown Button */
  .dropbtn:after {
    display: inline-block;
    width: 0;
    content: '';
    height: 0;
    margin-left: 6px;
    vertical-align: middle;
    border-top: 4px solid;
    border-right: 4px solid transparent;
    border-left: 4px solid transparent;
  }
  .dropbtn {
    font-family: 'Helvetica';
    background:none;
    cursor: pointer;
    color: #9d9d9d;
    text-align: center;
    text-decoration: none;
    line-height: 20px !important;
    position: relative;
    display: block;
    padding: 15px 15px ;
    margin: 0;
  }
  ul.topnav.nav-right-top {
      margin-right: 46px;
      float: right;
  }
  /* Dropdown button on hover & focus */
  .dropbtn:hover {
      color: #fff;
      background-color: transparent;
  }
   .dropbtn:focus{
    color: #fff;
    background-color: #080808;
    outline: none;
    border-radius:0;
  }

  /* The container <div> - needed to position the dropdown content */
  .dropdown {
      position: relative;
      display: inline-block;
  }

  /* Dropdown Content (Hidden by Default) */
  .dropdown-content {
    font-family: 'Helvetica';
    position: absolute;
    top: 100%;
    left: 0;
    z-index: 1000;
    display: none;
    float: left;
    min-width: 160px;
    padding: 5px 0;
    margin: 2px 0 0;
    list-style: none;
    font-size: 14px;
    text-align: left;
    background-color: #fff;
    border: 1px solid #ccc;
    border: 1px solid rgba(0,0,0,0.15);
    border-radius: 4px;
    -webkit-box-shadow: 0 6px 12px rgba(0,0,0,0.175);
    box-shadow: 0 6px 12px rgba(0,0,0,0.175);
    background-clip: padding-box;
        margin-top: 0;
    border-top-right-radius: 0;
    border-top-left-radius: 0;
  }
  ul.topnav li .dropdown-content li {
    float:left;
    width: 100%;
  }

  /* Links inside the dropdown */
  ul.topnav li .dropdown-content a {
    display: block;
    padding: 3px 20px;
    clear: both;
    font-weight: normal;
    line-height: 1.42857143;
    color: #333;
    white-space: nowrap;
  }

  /* Change color of dropdown links on hover */
  ul.topnav li .dropdown-content a:hover {
        text-decoration: none;
    color: #262626;
    background-color: #f5f5f5;
  }

     

  /* Show the dropdown menu (use JS to add this class to the .dropdown-content container when the user clicks on the dropdown button) */
  .show {display:block;}

  @media screen and (max-width:680px) {
    ul.topnav li:not(:first-child) {display: none;}
    ul.topnav li.icon {
      float: right;
      display: inline-block;
    }
  }

  /* The "responsive" class is added to the topnav with JavaScript when the user clicks on the icon. This class makes the topnav look good on small screens */
  @media screen and (max-width:680px) {
    ul.topnav.responsive {position: relative;}
    ul.topnav.responsive li.icon {
      position: absolute;
      right: 0;
      top: 0;
    }
    ul.topnav.responsive li {
      float: none;
      display: inline;
    }
    ul.topnav.responsive li a {
      display: block;
      text-align: left;
    }
  }

:javascript
  /* Toggle between adding and removing the "responsive" class to topnav when the user clicks on the icon */
  function myFunction() {
      var x = document.getElementById("myTopnav");
      if (x.className === "topnav") {
          x.className += " responsive";
      } else {
          x.className = "topnav";
      }
  }
  /* When the user clicks on the button, 
  toggle between hiding and showing the dropdown content */
  function myFunction() {
    document.getElementById("myDropdown1").classList.toggle("show");
  }

  function myFunction1() {
    document.getElementById("myDropdown2").classList.toggle("show");
  }

  function myFunction2() {
    document.getElementById("myDropdown3").classList.toggle("show");
  }

  function removeShowClass(){
    document.getElementById("myDropdown3").classList.remove("show");
    document.getElementById("myDropdown2").classList.remove("show");
    document.getElementById("myDropdown1").classList.remove("show");
  }

  // Close the dropdown menu if the user clicks outside of it
  window.onclick = function(event) {
    if (!event.target.matches('.dropbtn')) {

      var dropdowns = document.getElementsByClassName("dropdown-content");
      var i;
      for (i = 0; i < dropdowns.length; i++) {
        var openDropdown = dropdowns[i];
        if (openDropdown.classList.contains('show')) {
          openDropdown.classList.remove('show');
        }
      }
    }
  }